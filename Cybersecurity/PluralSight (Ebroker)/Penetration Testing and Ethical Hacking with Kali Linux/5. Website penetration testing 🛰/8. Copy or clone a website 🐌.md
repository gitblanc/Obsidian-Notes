For cloning a website, we use **HTTrack**

Download it:

````
sudo apt-get install httrack
````

Try the command:

````
httrack "http://ip_addr/mutillidae" -O "/tmp/httrack" -v
````

With this we can clone a complete website